#include "minishell.h"

int	exec_pipeline(t_cmd *cmds, t_shell *shell)
{
	(void)cmds;
	(void)shell;
	ft_putendl_fd("minishell: pipeline execution not implemented yet",
		STDERR_FILENO);
	return (1);
}
